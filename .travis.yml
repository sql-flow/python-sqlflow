language: python
sudo: false
cache:
  directories:
    - $HOME/.cache/pip
    
env:
  global:
    - TEST=-v

matrix:
  include:
    - python: 3.4
    - python: 3.5
    - python: 3.6
    - python: 3.7
      dist: xenial
      sudo: true
    - python: pypy

install:
  - pip install -U pip
  - pip install -r requirements.txt

script:
  - echo "OK"
